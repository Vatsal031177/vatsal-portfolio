[build]
# Removing NIXPACKS to allow Railway to use standard buildpacks
# which have better support for prebuilt native binaries.

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
